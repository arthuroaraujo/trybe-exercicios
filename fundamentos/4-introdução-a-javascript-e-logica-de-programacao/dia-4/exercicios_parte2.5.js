function indiceRepete (numeros){
    let maisRepete = []
    for(let indice in numeros){
        if(numeros[maisRepete] = numeros[indice]){
            maisRepete += 1
        };
    }
    return maisRepete;
}

